@page "/login"
@layout LetsTalk.Client.Views.Layout.LoginLayout
@using MudBlazor

<PageTitle>Connexion</PageTitle>



    <!-- “Fond” full page (couleur via thème, pas de gradient sans CSS) -->
    <MudContainer Square="true" Elevation="0" Class="mud-height-full mud-width-full d-flex align-center justify-center pa-6" style="height: 80vh;">

        <!-- “Shell” -->
        <MudContainer MaxWidth="MaxWidth.Large" Class="d-flex align-center justify-center">
            <MudPaper Outlined="true" Elevation="12" Class="pa-8">

                <MudGrid Spacing="0">

                    <!-- Gauche : branding -->
                    <MudItem xs="12" md="6" Class="d-flex align-center justify-center pa-8">
                        <MudStack Spacing="2" AlignItems="AlignItems.Center">

                            <MudAvatar Size="Size.Large" Color="Color.Primary" Variant="Variant.Filled">
                                <MudIcon Icon="@Icons.Material.Filled.ChatBubble" />
                            </MudAvatar>

                            <MudText Typo="Typo.h3">Let's Talk</MudText>

                            <MudText Typo="Typo.subtitle1" Color="Color.Secondary">
                                Connectez. Discutez. Collaborez.
                            </MudText>

                        </MudStack>
                    </MudItem>

                    <!-- Droite : carte login -->
                    <MudItem xs="12" md="6" Class="d-flex align-center justify-center">

                        <MudPaper Elevation="10" Class="pa-8" Width="420px">
                            <MudText Typo="Typo.h4" Class="mb-6">Connexion</MudText>

                            <MudForm @ref="_form">
                                <MudTextField @bind-Value="_email"
                                              Label="Email"
                                              Variant="Variant.Filled"
                                              Margin="Margin.Dense"
                                              FullWidth="true"
                                              Required="true"
                                              Adornment="Adornment.Start"
                                              AdornmentIcon="@Icons.Material.Filled.Email"
                                              Class="mb-4" />

                                <MudTextField @bind-Value="_password"
                                              Label="Mot de passe"
                                              Variant="Variant.Filled"
                                              Margin="Margin.Dense"
                                              FullWidth="true"
                                              Required="true"
                                              InputType="@(_showPassword? InputType.Text: InputType.Password)"
                                              Adornment="Adornment.End"
                                              AdornmentIcon="@(_showPassword? Icons.Material.Filled.VisibilityOff : Icons.Material.Filled.Visibility)"
                                              OnAdornmentClick="TogglePassword"
                                              Class="mb-6" />

                                <MudButton FullWidth="true"
                                           Size="Size.Large"
                                           Variant="Variant.Filled"
                                           Color="Color.Primary"
                                           OnClick="Submit">
                                    Se connecter
                                </MudButton>

                                <MudStack Direction="Row" Justify="Justify.SpaceBetween" Class="mt-4">
                                    <MudLink Href="/forgot" Underline="Underline.None">Mot de passe oublié ?</MudLink>
                                    <MudLink Href="/register" Underline="Underline.None">Créer un compte</MudLink>
                                </MudStack>
                            </MudForm>
                        </MudPaper>

                    </MudItem>

                </MudGrid>

            </MudPaper>
        </MudContainer>

    </MudContainer>

@code {
    private MudForm? _form;

    private string? _email;
    private string? _password;
    private bool _showPassword;

    private void TogglePassword() => _showPassword = !_showPassword;

    private async Task Submit()
    {
        if (_form is null) return;
        await _form.Validate();
        if (!_form.IsValid) return;

        // TODO: authentification + navigation
    }

}